<Window
    x:Class="Mantra.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Mantra"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Grid.Row="0" Text="Input URL:" />
            <TextBox
                Grid.Row="1"
                Width="600"
                Text="{Binding ImgUrl}" />
            <Button
                Width="100"
                Command="{Binding ShowCommand}"
                Content="Show" />
            <Button
                Width="100"
                Command="{Binding OCRCommand}"
                Content="OCR" />
            <Button
                Width="100"
                Command="{Binding TranslateCommand}"
                Content="Translate" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Margin="20,0" Text="Language:" />
            <RadioButton Content="English" GroupName="gn_Language" />
            <RadioButton
                Margin="20,0"
                Content="Japanese"
                GroupName="gn_Language"
                IsChecked="True" />
        </StackPanel>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Text="Source Image" />
            <TextBlock
                Grid.Column="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Text="Target Image" />
            <Image
                Grid.Column="0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Source="{Binding ImgSource}"
                Stretch="None" />
            <Image
                Grid.Column="2"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Source="{Binding ImgTarget}"
                Stretch="None" />
            <Canvas x:Name="canvas_1" Grid.Column="0" />
            <Canvas x:Name="canvas_2" Grid.Column="2" />
        </Grid>
    </Grid>
</Window>
